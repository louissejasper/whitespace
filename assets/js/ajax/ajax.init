( function( $ ) {
	$( document ).ready( function() {

		$.ajax( {
		    url : ajaxurl,
		    type : 'post',
		    data : {
		        action : 'action_name',
		        param_name : 'param_value'
		    },
		    success : function( response ) {
		        
		    }
		} );

	} );
} ( jQuery ) );